#include "mathoperations.h"

/**
 * subtraction - function to find the difference between two numbers
 * @fnum: the first number
 * @snum: the second number to minus from the first
 * Return: difference
 */

double subtraction(double fnum, double snum)
{
	return (fnum - snum);
}
