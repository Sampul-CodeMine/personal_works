#include "mathoperations.h"

/**
 * multiplication - function to find the products of two numbers
 * @fnum: the first number to multiply
 * @snum: the second number to multiply
 * Return: product of the two numbers
 */

double multiplication(double fnum, double snum)
{
	return (fnum * snum);
}
