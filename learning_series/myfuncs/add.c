#include "headers.h"

/**
 * addition - this function add two (2) numbers
 * @first: first parameter (double) 
 * @second: second parameter (double)
 * Return: the sum
 */

double addition(double first, double second)
{
	double sum = 0;

	sum = first + second;
	return (sum);
}
