#include "headers.h"

/**
 * percent - this function finds the percentage of a number
 * @number: parameter to find its percentage (double)
 * Return: the percentage
 */

double percent(double number)
{
        double percnt = 0;

        percnt = (number / 100);
        return (percnt);
}
