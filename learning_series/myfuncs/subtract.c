#include "headers.h"

/**
 * subtract - this function finds the difference between two (2) numbers
 * @first: first parameter (double)
 * @second: second parameter (double)
 * Return: the difference
 */

double subtract(double first, double second)
{
        double difference = 0;

        difference = first - second;
        return (difference);
}
