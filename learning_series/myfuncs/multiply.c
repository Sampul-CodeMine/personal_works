#include "headers.h"

/**
 * multiply - this function finds the product of two (2) numbers
 * @first: first parameter (double)
 * @second: second parameter (double)
 * Return: the product
 */

double multiply(double first, double second)
{
        double product = 0;

        product = first * second;
        return (product);
}
